import React from "react";
import s from "./ErrorMessage.module.css";

const ErrorMessage = () => {
  return <div className={s.text}>Щось сталось. Онови сторінку...</div>;
};

export default ErrorMessage;
